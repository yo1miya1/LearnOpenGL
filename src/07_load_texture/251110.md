##### 2025.11.10

######  	01-读取纹理

 		纹理采样。
######  	02-读取纹理
		渲染循环中：
			//glActiveTexture(GL_TEXTURE0);
			//glBindTexture(GL_TEXTURE_2D, TexVarName);
			如果只有一个纹理单元，这两句可以默认不写。
			但是如果读取多个纹理，就需要手动激活，并绑定
######  	03-作业
		1、2、3都顺利的完成。
		4、需要获取按键输入来调整数值，暂时无法完成。
		但是可以通过传参数实现效果。
